<Window x:Class="NoxyTools.Wpf.Views.ListEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NoxyTools.Wpf.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ListEditorViewModel, IsDesignTimeCreatable=False}"
        Title="목록 편집기"
        Width="680" Height="500"
        MinWidth="520" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        Background="{StaticResource PanelBackgroundBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── 3열 편집 영역 ── -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="84"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ── 왼쪽 패널 ── -->
            <Border Grid.Column="0"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="6">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top"
                               Text="{Binding LeftTitle}"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource AccentTextBrush}"
                               Margin="0,0,0,4"/>
                    <TextBox DockPanel.Dock="Top"
                             Text="{Binding LeftFilter, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,4"/>
                    <ListBox x:Name="LeftListBox"
                             ItemsSource="{Binding LeftView}"
                             Background="{StaticResource InputBackgroundBrush}"
                             BorderThickness="0"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{StaticResource PrimaryTextBrush}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="4,2"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <EventSetter Event="MouseDoubleClick"
                                             Handler="LeftItem_DoubleClick"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource SelectionBackgroundBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- ── 중앙 버튼 ── -->
            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center"
                        Margin="8,0">
                <Button Content="추가 →"
                        Command="{Binding AddCommand}"
                        CommandParameter="{Binding SelectedItem, ElementName=LeftListBox}"
                        Margin="0,0,0,8"
                        Padding="4,6"/>
                <Button Content="← 삭제"
                        Command="{Binding RemoveCommand}"
                        CommandParameter="{Binding SelectedItem, ElementName=RightListBox}"
                        Padding="4,6"/>
            </StackPanel>

            <!-- ── 오른쪽 패널 ── -->
            <Border Grid.Column="2"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="6">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top"
                               Text="{Binding RightTitle}"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource AccentTextBrush}"
                               Margin="0,0,0,4"/>
                    <TextBox DockPanel.Dock="Top"
                             Text="{Binding RightFilter, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,4"/>
                    <ListBox x:Name="RightListBox"
                             ItemsSource="{Binding RightView}"
                             Background="{StaticResource InputBackgroundBrush}"
                             BorderThickness="0"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{StaticResource PrimaryTextBrush}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="4,2"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <EventSetter Event="MouseDoubleClick"
                                             Handler="RightItem_DoubleClick"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource SelectionBackgroundBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource HoverBackgroundBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>

        <!-- ── 하단 버튼 ── -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="확인"
                    Command="{Binding ConfirmCommand}"
                    Width="80" Height="28"
                    Margin="0,0,8,0"/>
            <Button Content="취소"
                    Click="CancelButton_Click"
                    Width="80" Height="28"/>
        </StackPanel>
    </Grid>
</Window>
