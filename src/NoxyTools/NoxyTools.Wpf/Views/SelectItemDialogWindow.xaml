<Window x:Class="NoxyTools.Wpf.Views.SelectItemDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NoxyTools.Wpf.ViewModels"
        xmlns:conv="clr-namespace:NoxyTools.Wpf.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:SelectItemDialogViewModel, IsDesignTimeCreatable=False}"
        Title="아이템 선택"
        SizeToContent="WidthAndHeight"
        MaxWidth="900"
        MaxHeight="600"
        MinWidth="200"
        MinHeight="120"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource PanelBackgroundBrush}">

    <Window.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 아이템 버튼 목록 (WrapPanel) -->
        <ScrollViewer Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      MaxHeight="520">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ButtonItemVM}">
                        <Button Command="{Binding DataContext.SelectItemCommand,
                                         RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Width="120" Height="80"
                                Margin="4"
                                ToolTip="{Binding Text}">
                            <StackPanel VerticalAlignment="Center">
                                <Image Source="{Binding Image}"
                                       Width="40" Height="40"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="NearestNeighbor"
                                       Visibility="{Binding Image,
                                           Converter={StaticResource NullToVisibilityConverter}}"/>
                                <TextBlock Text="{Binding Text}"
                                           TextWrapping="Wrap"
                                           TextAlignment="Center"
                                           MaxWidth="110"
                                           FontSize="10"
                                           Foreground="{StaticResource PrimaryTextBrush}"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 취소 버튼 -->
        <Button Grid.Row="1"
                Content="취소"
                Click="CancelButton_Click"
                Width="80" Height="28"
                HorizontalAlignment="Right"
                Margin="0,8,0,0"/>
    </Grid>
</Window>
